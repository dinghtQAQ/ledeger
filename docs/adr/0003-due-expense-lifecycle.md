# 到期支出使用明确的生命周期

**Status: accepted**

到期支出与普通支出区分处理，生命周期只有 `unpaid`、`paid` 和 `cancelled` 三种状态。付款只改变状态，不新建一笔重复支出；冲正会把原到期支出置为 `cancelled`，被冲正的账目不能再付款。这个状态机保留了“已确认的未来义务”和“已经完成付款”之间的差异，同时让冲正后的账目不再继续改变。

## Consequences

- 到期支出必须有到期时间，普通收入和支出不能携带到期时间。
- 付款操作需要具备幂等性，重复付款不会产生额外账目。
- 分期、利息和自动扣款不属于当前状态机。

# ledeger Agent Instructions

尽可能用中文回答用户。查阅代码等文件时，先从搜索开始再局部读取文件，除非必要不全部读取文件。

## Agent skills

### Issue tracker

Issues and specs live in GitHub Issues for `dinghtQAQ/ledeger`; local ticket copies are not maintained. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the default triage vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repo using root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
